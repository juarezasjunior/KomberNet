<RadzenFormField Text="@this.FormattedDescription">
    <RadzenStack>
        @ChildContent

        @if (this.IsRequired)
        {
            <RadzenRequiredValidator Component="@this.FieldName" Text="@this.Localizer["Field_Required"]" />
        }
    </RadzenStack>
</RadzenFormField>